# studio/workflows

这里放报告生成流程。

每个 workflow 描述一类产品线从输入到输出的步骤，例如：

- `growth-breakthrough.yaml`：增长问题破局
- `annual-strategy.yaml`：年度战略规划
- `competition-analysis.yaml`：竞争格局分析

workflow 只描述编排，不直接写页面内容，也不存行业素材。

当前已开始落地：

- `growth-breakthrough.yaml`：先跑通第 1 页封面切片。
