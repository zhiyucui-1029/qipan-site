# brand-kits

这里放可复用品牌色包。

注意：用户一次性上传的颜色，默认应保存在具体项目输出或后端数据库里，不直接进入 git。这里主要放我们主动维护的样板品牌色包，例如：

- qipan.site 默认品牌色
- 耐克中国样张色彩映射
- 咖啡连锁样张色彩映射

品牌色包只放颜色和使用规则，不放图片素材，也不放 PPT 页面内容。

当前默认品牌色包：

- `qipan-site.yaml`：qipan.site 默认深绿、深蓝、哑金咨询报告风。
